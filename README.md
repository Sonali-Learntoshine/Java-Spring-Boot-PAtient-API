# Java-Spring-Boot-PAtient-API

This is a API developed using Spring Boot, Lombok, JPA Repository, Mysql.

Only backend operations are performed in it which can be accessible by both Swagger and Postman.
